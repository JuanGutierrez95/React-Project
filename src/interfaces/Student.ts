/*Definimos una interfaz llamada StudentsProps que especifica que el componente espera recibir una propiedad llamada name de tipo string. */
export default interface StudentsProps {
    name: string;
  }